QUAN TRỌNG: Trong quá trình viết có thể có thiếu sót hãy bổ sung, sửa bất kì phần còn thiếu nếu thấy bất hợp lý
I.Về hàm main:
Có 3 chương trình chính:
1. Gọi method test trong InOutput và nếu method test chạy vô hiệu hóa các chương trình còn lại để chạy test.
Yêu cầu truyền vào (Luachon Boolean)
2. True: Gọi method BuyTicket() của ServiceTicker.
3. False Gọi method CannerTicket() của ServiceTicker.
4. Null : Thoát chương trình
II. Về service: Bổ sung thêm ngoại lệ nếu thiếu
1. ServiceUser thêm method findUser theo idUser, thêm InUser
2. ServiceSeat 
3. ServiceTicker thêm method BuyTicket() chạy như sau: Yêu cầu truyền vào Luachon (Boolean)
* -> Nếu True -> Gọi createUser() nhập chosse và thuộc tính user -> (Đoạn chạy tiếp **) -> Gọi method InAllTrips của Trip -> Truyền vào idTrip -> Gọi method InAllSeats -> Truyền vào idSeat -> Gọi InTicker -> Truyền thuộc tính phù hợp vào -> Gọi method ThanhToan trong ServicePayment.
* -> Nếu False -> Gọi method InUser -> Nếu trả về True thì chạy "Đoạn chạy tiếp **"
                                    -> Nếu trả về True thì chạy StartService trong StaffService.

Thêm method Canner để hủy chuyến yêu cầu truyền vào IdUser sau đó in ra các chỗ sau đó yêu cầu IdSeat để chọn chuyến cần hủy truyền vào boolean true để hủy false để không hủy
4. ServiceTrip
5. ServicePayment Yêu cầu chọn trong khoảng (1-3 như ServiceUser) để chọn cách thanh toán của 3 interface ChuyenKhoan, TienMat, Vi. TienMat yeu cau so tien neu so tien truyền vào > getTickerPrice() của user đó thì hiện true và In ra ("Thanh toan thanh cong") nếu không thì in ra ("Thanh toan khong thanh cong") kết thúc chương trình. Với hai phương thức thanh toán còn lại thì tương tự nhưng với ChuyenKhoan yêu cầu idTK, Vi thì cần IdVi + maPIN
6. InOutput tạo method test method này sẽ kiểm tra fodel repository có file nào là file .JSON không và gọi các IO ở repository lên để xử lý. Sau đó dựa vào các file test .json đó để chạy tình huống test.
III. Về repository (dùng để chạy các test): Tạo các file test cho các tình huống sau:
• Khách đặt ghế còn trống thành công
• Khách đặt ghế đã có người đặt  
• Khách chọn ghế không tồn tại  
• Khách VIP đặt ghế VIP và được tính giá đúng  
• Thanh toán vé đã đặt 
1. PaymentIO tạo method nhận file.Json nằm chung packet repository chứa các dữ liệu cần thiết cho ServicePayment bằng Gson
2. TickerIO tạo method nhận file.Json nằm chung packet repository chứa các dữ liệu cần thiết cho ServiceTicker bằng Gson
3. UserIO tạo method nhận file.Json nằm chung packet repository chứa các dữ liệu cần thiết cho ServiceUser bằng Gson
4. TripIO tạo method nhận file.Json nằm chung packet repository chứa các dữ liệu cần thiết cho ServiceTrip bằng Gson